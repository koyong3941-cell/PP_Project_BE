<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.pp.plant.model.dao.PlantReviewImgMapper">

	<insert id="insertPlantReviewImg">
		INSERT 
	      INTO 
	     		PLANT_REVIEW_IMG 
	   	    	(
				REVIEW_NO
		 	 , 	ORIGINAL_NAME
			 , 	SAVE_NAME
			 , 	IMG_PATH
			 , 	IMG_ORDER
			    )
	    VALUES 
	        	(
				#{reviewNo}
			 , 	#{originalName}
			 , 	#{saveName}
			 , 	#{imgPath}
			 , 	#{imgOrder}
			    )
	</insert>
	
	<select id="findImagesByReviewNos">
		SELECT
			 	REVIEW_NO
			 ,	ORIGINAL_NAME
			 ,	SAVE_NAME
			 ,	IMG_PATH
			 ,	IMG_ORDER
		  FROM
		  		PLANT_REVIEW_IMG
		 WHERE
		 		REVIEW_NO IN
		<foreach collection="reviewNos" item="reviewNo" open="(" close=")" separator=",">
				#{reviewNo}
		</foreach>
		   AND
		   		DEL_YN = 'N'
		 ORDER
		 	BY
		 		REVIEW_NO, IMG_ORDER ASC
	</select>
	
	<update id="deletePlantReviewImgByReviewNo">
		UPDATE
				PLANT_REVIEW_IMG
		   SET
		   		DEL_YN = 'Y'
		 WHERE
		 		REVIEW_NO = #{reviewNo}
	</update>

</mapper>